# Data

This directory is reserved for project-level shared datasets, annotations, and lightweight samples.

The imported legacy project keeps its original data under `baselines/vision_obstacle_avoidance_legacy/data/` so the previous work remains self-contained. Future shared datasets can be promoted here when their format is stable.

Large data is tracked through Git LFS when committed.
