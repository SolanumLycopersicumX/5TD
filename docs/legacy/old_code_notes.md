# Legacy Code Notes

The imported older pure-vision project is preserved under `baselines/vision_obstacle_avoidance_legacy/`.

The active RGB-only engineering baseline is `baselines/hbdnet_rt/`. New pure-RGB engineering work should generally start there or in `src/tunnel_nav/` once interfaces are stable.
