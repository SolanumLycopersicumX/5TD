# Architecture Notes

This directory is for system architecture, safety constraints, interfaces, and roadmaps.

Keep implementation details in code-level docs and experiment results in `experiments/`.
